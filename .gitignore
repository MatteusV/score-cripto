# Dolt database files (added by bd init)
.dolt/
*.db

# Node services
node_modules/
dist/

# Environment files
.env
.env.local

/src/generated/
.pnpm-store

# E2E test config (generated at runtime)
**/tests/e2e/helpers/.e2e-config.json

# RSA key pair (generated by pnpm keys:generate)
keys/
*.pem

# Beads / Dolt files (added by bd init)
.beads-credential-key

# Stuck root-owned generated dirs from prior Docker runs (cannot rm without sudo)
services/*/src/.generated-stuck/
.vercel
.env*.local
.tmp-plans/
# Added by code-review-graph
.code-review-graph/
