API_BASE_URL="http://localhost:3001"
# api-gateway proxia /auth/* + /billing/* + /profile para users via Flycast.
# Em dev pode apontar pro api-gateway local; em prod, score-cripto-api-gateway.fly.dev.
API_GATEWAY_URL="http://localhost:3001"
# DEPRECATED: legado da época em que web-app falava direto com users.
# Mantido como fallback enquanto API_GATEWAY_URL é roleado em prod (Vercel).
# Remover após PR 3 (users vira Flycast-only).
USERS_URL="http://localhost:3003"
DATA_INDEXING_URL="http://localhost:4000"
DEEPL_API_KEY=""
